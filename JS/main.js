/* 
JavaScript rules:
**const value cannot be changed later.
**let (var name) can be changed later.
**alert() will be shown to client. Use console.log()
  for anything else.
*/

let warning = 'You can edit this alert after forking. It will appear every time you load this page.';

alert(warning);
